# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# data storage (leads, contacts)
/.data

# Payload uploads (local media)
/media

# Payload generated types — committed so Vercel build has them.
# Regenerate locally with `npm run generate:types` after editing collections.

# vercel
.vercel

# source/reference materials (not part of codebase)
Manual_da_Marca_KRIATIVA_v2*.html
Plano de Negócios*.md

# typescript
*.tsbuildinfo
next-env.d.ts
